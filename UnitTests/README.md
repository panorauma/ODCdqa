## Unit Tests

Am using:

- `functions_test.R` to test functions in `functions.R`
- Docker container to test **pandas-profiling** (all Python versions after 3.11.x not supported)
  - all req packages should be listed in `requirements.txt`
